"use client";

import { LegacyRouteRedirect } from "@/components/LegacyRouteRedirect";

// ============================================================
// LOGIN — DISABLED
// ============================================================
// Login is currently bypassed. Redirect to the student workspace.
// ============================================================

export default function Page() {
  return <LegacyRouteRedirect />;
}
